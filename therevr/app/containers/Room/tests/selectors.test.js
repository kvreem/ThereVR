// import { fromJS } from 'immutable';
// import { makeSelectRoomDomain } from '../selectors';

// const selector = makeSelectRoomDomain();

describe('makeSelectRoomDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
