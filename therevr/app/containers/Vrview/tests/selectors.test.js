// import { fromJS } from 'immutable';
// import { makeSelectVrviewDomain } from '../selectors';

// const selector = makeSelectVrviewDomain();

describe('makeSelectVrviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
